#pragma once

#include <iostream>
#include <string>

namespace bopt {
/// @brief Real value type
using Real = double;
/// @brief Indexing type for arrays and vectors
using Index = long int;
/// @brief Type to represent the dimensions of objects
using Size = std::size_t;
/// @brief String type
using String = std::string;

}  // namespace bopt
