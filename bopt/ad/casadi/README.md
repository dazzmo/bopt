## Casadi Interface
Casadi offers automatic differentation capabilities, as well as powerful code generation which we can take advantage of.

The CasADi interface in BOPT allows users to:
* Create expression classes based on casadi expressions, such as differentiable expressions and structured expressions (e.g. linear or quadratic forms)
* Create compiled code-generated functions that can be created and loaded at runtime. 